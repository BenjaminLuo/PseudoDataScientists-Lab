import RestaurantList from './RestaurantList';

export default function RestaurantScreen() {
    return (
        <div>
            <h1>This is the Restaurants screen</h1>
            <RestaurantList />
        </div>
    );
}